<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Asia/Kolkata timezone for all timestamps -->
    <timestamp key="startTimestamp" datePattern="yyyy-MM-dd_HH-mm-ss" timeZone="Asia/Kolkata"/>

    <property name="LOG_DIR" value="${LOG_DIR:-src/main/resources/logs}" />
    <property name="LOG_FILE" value="${LOG_DIR}/service-user.log" />
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS, Asia/Kolkata} [%-5level] [%thread] [%logger{36}] [%X{traceId:-NO_TRACE}] - %msg | status=%X{statusCode:-N/A}%n%ex{full}" />

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- File Appender with rolling policy -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/service-user-%d{yyyy-MM-dd, Asia/Kolkata}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Application-specific loggers -->
    <logger name="com.grocery.service_user" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- Spring framework loggers -->
    <logger name="org.springframework" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- Hibernate logger -->
    <logger name="org.hibernate" level="WARN" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>
